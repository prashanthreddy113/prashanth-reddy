<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Speech-to-text goes through the system RecognizerIntent; RECORD_AUDIO is declared
         so the app can later move to an in-app SpeechRecognizer without a manifest change. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Because CAMERA is declared here, the app must hold it at runtime before launching
         ACTION_IMAGE_CAPTURE (see ui/components/PhotoButton.kt). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- The phone buzzes when a new ride request arrives (HomeScreen). -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Location + foreground service permissions are declared now so a later step can add
         the "captain is online" location foreground service (LocationService) without a
         manifest change. Nothing in this demo requests location yet and no service is
         declared. When the service is added it needs foregroundServiceType="location" and
         ACCESS_FINE_LOCATION must be requested at runtime before it starts. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />

    <!-- Package visibility (Android 11+): let us discover speech / TTS / camera / maps apps. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <!-- 🧭 "Show the way" opens Google Maps navigation, falling back to any geo: handler. -->
        <package android:name="com.google.android.apps.maps" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
    </queries>

    <application
        android:name=".ManaBandiCaptainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.ManaBandiCaptain">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ManaBandiCaptain.Splash"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lets AppCompat persist the per-app locale on Android 12 and below. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>

</manifest>
